{
  "id": "cyberpunk_07_optimize",
  "paradigm": "CYBERPUNK",
  "name": "Overloaded Mainframe Bypass",
  "description": "Optimize the recursive CPU-intensive security function to run within the mainframe's operation budget. The system allows at most 50 operations to compute fib(10).",
  "logos_token": "LOGOS_CPK_FIB_OPTIMIZE_01",
  "skill_type": "optimize",
  "template_code": "function fib(n) {\n    if (n <= 1) return n;\n    return fib(n - 1) + fib(n - 2);\n}",
  "validation_script": "// Count function-level calls via a wrapper — works for both iterative and recursive.\n// O(2^N) recursion at n=10 expands to 177 unique fib() calls tracked by ops.\n// O(N) iteration loops internally without re-calling fib, so ops == 1.\n// We test correctness first, then enforce the budget.\nfunction wrappedFib(n) {\n    incrementOp();\n    if (n <= 1) return n;\n    return wrappedFib(n - 1) + wrappedFib(n - 2);\n}\nlet recursiveOps;\n(function() {\n    wrappedFib(10);\n    recursiveOps = getOpsCount();\n})();\nlet result = fib(10);\nif (result !== 55) throw new Error('Incorrect output for fib(10): got ' + result + '. Expected 55.');\nif (fib(30) !== 832040) throw new Error('Incorrect output for fib(30)');\nif (fib(0) !== 0) throw new Error('Edge case: fib(0) should be 0');\nif (fib(1) !== 1) throw new Error('Edge case: fib(1) should be 1');\n// Measure player ops by counting actual calls they use internally\n// Compare against the reference recursive cost — if player uses fewer ops, they optimized.\nif (recursiveOps > 50) {\n    // This is the REFERENCE recursive: 177 ops expected. That's fine.\n}\n// Now measure the PLAYER's approach by wrapping their function.\nvar playerOps = 0;\nfunction countedFib(n) {\n    playerOps++;\n    if (n <= 1) return n;\n    return countedFib(n - 1) + countedFib(n - 2);\n}\n// Rebuild player's logic inline-equivalent for counting:\n// We can't re-wrap fib() without hitting scoping issues in Goja,\n// so we test complexity by checking: is fib(20) correct AND fast?\n// The structural check: a correct O(N) solution will solve fib(40) before timeout.\nif (fib(40) !== 102334155) throw new Error('Incorrect output for fib(40)');\nsetState('cpu_overload', false);\nsetState('security_bypass', true);",
  "initial_state": {
    "cpu_overload": true,
    "security_bypass": false
  },
  "flaws": [
    {
      "id": "optimize_script",
      "trigger_event": "execute_script",
      "name": "Upload Optimized Script",
      "flavor_text": "Run the optimized script in the mainframe sandbox.",
      "conditions": {},
      "mutations": {},
      "fallback_mutations": {}
    }
  ],
  "win_condition": {
    "target_state_key": "security_bypass",
    "expected_value": true
  }
}
